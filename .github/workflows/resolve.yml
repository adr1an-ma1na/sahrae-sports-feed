name: Resolve sports feed

on:
  schedule:
    - cron: '*/10 * * * *'   # every 10 minutes
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: resolve
  cancel-in-progress: true

jobs:
  resolve:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Publish schedule + embed URLs
        run: node resolve.mjs

      # NOTE: DaddyLive (daddy.py) is intentionally NOT run here. Its resolution
      # domains (dlhd.dad / daddylive.dad / thedaddy.to) DNS-fail from GitHub
      # Actions runners (blocked), so CI resolves 0/220 channels. It can only work
      # resolved ON-DEVICE (residential IP). Kept in the repo for that future path.

      - name: Show feed summary
        if: always()
        run: |
          grep -oE '"count":[0-9]+|"resolved":[0-9]+|"base":"[^"]*"' feed.json || true
          head -c 400 feed.json || true

      - name: Commit feed
        if: always()
        run: |
          if [ ! -f feed.json ]; then echo "no feed.json"; exit 0; fi
          git config user.name "sahrae-feed-bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add feed.json
          git commit -m "feed update $(date -u +%FT%TZ)" || { echo "no changes"; exit 0; }
          git push
